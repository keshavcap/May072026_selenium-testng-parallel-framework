# Application Configuration
base.url=https://the-internet.herokuapp.com/
implicit.wait=5
explicit.wait=15
page.load.timeout=30

# Browser defaults (can be overridden by testng.xml parameter)
default.browser=chrome
headless=false
